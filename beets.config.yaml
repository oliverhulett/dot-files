library: ~/Music/library.db
directory: ~/Music
plugins:
  #- gmusic
  #- lyrics
  #- replaygain
- chroma
- convert
- duplicates
- edit
- fetchart
- lastimport
- missing
- random
- the

import:
  move: yes
  copy: no
  link: no
  hardlink: no
  resume: ask
  incremental: yes
  log: ~/.cache/beets.log
  autotag: yes
  from_scratch: no
  default_action: apply
  detail: yes
  duplicate_action: keep

paths:
  default: Sorted/Music.%lower{${format}}/%the{${albumartist}}/%if{${year},${year}-}${album}/%if{${disctotal} = 1,Disc_${disc}}/%if{${track},${track}-}$title
  singleton: Sorted/Music.%lower{${format}}/%if{${year},${year}-}%the{${artist}}/${title}
  comp: Sorted/Music.%lower{${format}}/%if{${year},${year}-}${album}/%if{${disctotal} = 1,,Disc_${disc}}/%if{${track},${track}-}%if{${artist},%the{${artist}}-}${title}

clutter: [Thumbs.DB, .DS_Store, cover.jpg, cover.png]
ignore:
- .*
- '*~'
- System Volume Information
- lost+found
ignore_hidden: yes

replace:
  '[\\/]': _
  ^\.: _
  '[\x00-\x1f]': _
  '[<>:"\?\*\|]': _
  \.$: _
  \s+$: ''
  ^\s+: ''
  ^-: _
  '[^a-zA-Z0-9-.]+': _
path_sep_replace: _
asciify_paths: yes
art_filename: cover

threaded: yes
timeout: 5.0
per_disc_numbering: yes
verbose: 0
original_date: yes

lastfm:
  user: dontpanic
