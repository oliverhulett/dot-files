#!/bin/bash -asdf
# first line
# This file is a dummy file-under-test for use as a TEST_FILE= value when testing `assert_prog` and `find_test_file`.
# This file exists because the file-under-test has to be a file checked into the git repository, so we can't create them on the fly.
