## OneTick binaries are installed in /optiver, but we need to export the location of the configuration.
## I've put a copy in ~/etc/onetick/, so we'll export that.
ONE_TICK_CONFIG="${HOME}/etc/onetick/one_tick_config.txt"
export ONE_TICK_CONFIG
